# AWS Textract document text extraction demo
boto3>=1.34.0          # AWS SDK for Python (Textract client)
ipywidgets>=8.0.0      # File upload widget inside the notebook
Pillow>=10.0.0         # Image loading / preview
pdf2image>=1.17.0      # (optional) render a PDF page to an image for preview
